//By aMeYa PrAbHu
#include <bits/stdc++.h>
using namespace std;

#define ll long long int
typedef pair<ll,ll> pii;
typedef vector<ll> vi;
typedef vector< pii > vpii;

#define mp make_pair
#define pb push_back
#define mt make_tuple
#define F first
#define S second
#define sz(x) (ll)(x.size())
#define endl '\n'
#define fa(i,x,y) for(ll i=x; i<y; ++i)
#define fd(i,x,y) for(ll i=x-1; i>=y; --i)
#define rep(i,x) for(ll i=0; i<x; ++i)
#define in1(x) cin>>x
#define in2(x,y) cin>>x>>y
#define in3(x,y,z) cin>>x>>y>>z
#define ina(x,n); rep(i,n)cin>>x[i];
#define outs1(x) cout<<x<<" "
#define outs2(x,y) cout<<x<<" "<<y<<" "
#define outs3(x,y,z) cout<<x<<" "<<y<<" "<<z<<" "
#define outas(x,n); rep(i,n)cout<<x[i]<<" ";
#define outn(a) cout<<a<<endl;
#define outan(a,n); rep(i,n)cout<<a[i]<<endl;
#define all(x) x.begin(),x.end()
#define ___ ios_base::sync_with_stdio(false);cin.tie(NULL);cout<<fixed<<setprecision(16);
#define mset(x,y) memset(x,y,sizeof(x))
#define sortv(x) sort(x.begin(),x.end())
#define sorta(x,y) sort(x,x+y);
#define tst()  int t; cin>>t; while(t--)
#define tr(c,i) for(typeof((c).begin() i = (c).begin(); i != (c).end(); i++) 
#define present(c,x) ((c).find(x) != (c).end())
#define max3(x,y,z) max(x,max(y,z))
#define max4(w,x,y,z) max(w,max3(x,y,z))
#define INF 1e15
#define N 100100   

inline ll pwr(ll x,ll y,ll p)
{
    ll z=1;
    for(;y;y>>=1,x=x*x%p)if(y&1)z=z*x%p;
    return z;
}


int main()
{
 ___
 
  
    
  return 0;
}

 
